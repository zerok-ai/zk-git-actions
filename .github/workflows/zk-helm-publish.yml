# Common action: to be called by other actions
name: helm-publish
on:
  workflow_call:
    inputs:
      HELM_VERSION:
        type: string
        required: true
      DOCKER_IDENTIFIER:
        type: string
        required: true
      ENVIRONMENT:
        type: string
        default: 'poc'
      HELM_BUCKET_NAME:
        type: string
      HELM_BUCKET_URL:
        type: string
      PROJECT_ID:
        type: string
      APP_NAME_VAR:
        type: string
        default: 'APP_NAME'
    secrets:
      AWS_ACCESS_ID:
        required: true
      AWS_SECRET_KEY:
        required: true
env:
  HELM_VERSION: ${{ inputs.HELM_VERSION }}
  APP_NAME_VAR: ${{ inputs.APP_NAME_VAR }}
  ENVIRONMENT: ${{ inputs.ENVIRONMENT }}
  DOCKER_REG: ${{ vars.DOCKER_REG }}
  PROJECT_ID: ${{ inputs.PROJECT_ID }}
  DOCKER_REPO: ${{ vars.DOCKER_REPO }}
  DOCKER_IDENTIFIER: ${{ inputs.DOCKER_IDENTIFIER }}

jobs:
  helm-publish:
    runs-on: zk-self-hosted
    env:
      APP_NAME: ${{ vars[format('{0}', inputs.APP_NAME_VAR)] }}
    environment:
      name: ${{ inputs.ENVIRONMENT }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_KEY }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: s3 sync repo
        run: |-
          echo "APP_NAME=$APP_NAME"
          aws s3 sync --debug ${{ inputs.HELM_BUCKET_NAME }}/zk-client/$APP_NAME .

      - name: prepare-helm-values
        run: |-
          DOCKER_REPO=$DOCKER_REG/$PROJECT_ID/$DOCKER_REPO/$DOCKER_IDENTIFIER
          perl -pi -e "s/tag: \"latest\"/tag: \"$HELM_VERSION\"/" helm-charts/values.yaml
          perl -pi -e "s/migrationTag: \"latest\"/migrationTag: \"$HELM_VERSION\"/" helm-charts/values.yaml
          perl -pi -e "s#dockerBase: \".*\"#dockerBase: \"$DOCKER_REPO\"#" helm-charts/values.yaml

      - name: Uploading Helm Charts (for deploy)
        uses: actions/upload-artifact@v3
        with:
          name: helm-charts
          path: |
            helm-charts

      - name: helm package
        run: |-
              helm dependency update helm-charts
              helm package --version $HELM_VERSION helm-charts
      - name: helm index
        run: |-
              helm repo index ./ --url ${{ inputs.HELM_BUCKET_URL }}/zk-client/$APP_NAME/
      - name: s3 sync
        run: |-
             aws s3 cp . ${{ inputs.HELM_BUCKET_NAME }}/zk-client/$APP_NAME  --recursive --exclude "*" --include "index.yaml"  --include "*.tgz" --exclude "*/*"

      - name: Clear CF Cache
        run: aws cloudfront create-invalidation --distribution-id ${{ vars.CF_DIST_ID }} --paths "/*"

     
